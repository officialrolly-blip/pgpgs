import type { Metadata } from "next";
import PageShell from "@/components/page-shell";

export const metadata: Metadata = { title: "Clean Up Drives" };

export default function Page() {
  return <PageShell title="Clean Up Drives" />;
}
